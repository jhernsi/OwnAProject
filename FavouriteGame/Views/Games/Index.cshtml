@using GameDBModel;
@model IEnumerable<Game>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Games</h2>

<table width="50%">
    <tr>
        <th>Title</th>
        <th>Platform</th>
        <th>...</th>
    </tr>

    @foreach (Game g in Model)
    {
        <tr>
            <td>@g.Title</td>
            <td>@g.Platform</td>
            <td> @Html.ActionLink("Modify", "Edit", new { id = g.Id })
             @Html.ActionLink("Detail","Details",new  {id = g.Id}) 
            @Html.ActionLink("Delete", "Delete", new { id = g.Id }) Delete ???</td>
        </tr>
    }


</table>












